# Electron_Webscraping
To Run the app:
1.) Open the folder 'Electron test' within vs code
2.) Select/open a js file within vscode
3.) Open a js terminal
4.) Enter the following text into the terminal (and press enter) 'npm run start'